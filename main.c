#include "graph.h"
#include "list.h"
#include <stdio.h>

int main(int argc, char const *argv[]) {
  fprintf(stdout, "%s\n", (char*)"------ graph with adjacency list implementation ------");
  return 0;
}
